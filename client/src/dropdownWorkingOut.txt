  let comparison = 0;
  if(this.state.qualification!=""){
    if (qualificationA.education && qualificationA.qualification == this.state.qualification) comparison = -1;
    else if(qualificationA.education === false && qualificationA.industry == this.state.industry) comparison = -1;
    else if (qualificationB.education && qualificationB.qualification == this.state.qualification) comparison = +1;
    else if (qualificationB.education === false && qualificationB.industry == this.state.industry) comparison = +1;
    else comparison = 0;
  }
  return comparison;

  var groupedPosts=this.state.groupedPosts
  for(var i=0; i<groupedPosts.length; i++)
  {
    groupedPosts[i]= groupedPosts[i].sort(this.sortByQualification)
  }
  this.setState({groupedPosts: groupedPosts})